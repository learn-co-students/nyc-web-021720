import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// import export syntax 
// default vs. named exports
// default exports can be imported with any name and without curly brackets
// named exports need curlyBois and need to match export name exactly 

ReactDOM.render( <App />, document.getElementById('root') );

